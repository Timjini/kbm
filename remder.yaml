databases:
  - name: kbm
    databaseName: kbm
    user: kbm

services:
  - type: web
    name: kbm
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: 33ed00edf9cfe8ee2c33f142bbd692af
        sync: false